import 'package:flutter/animation.dart';

class Rooms {
  late String id, name, nameEn, price, bio, bioEn;
  Color color;
  Rooms({
    required this.id,
    required this.name,
    required this.nameEn,
    required this.price,
    required this.bio,
    required this.bioEn,
    required this.color,
  });
}
